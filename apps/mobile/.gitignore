
# NOTE: expo-env.d.ts is intentionally committed (not ignored) so CI's
# `tsc --noEmit` resolves Expo's EXPO_PUBLIC_* ambient types without first
# running the Expo TypeScript plugin. (Expo's template normally ignores it.)
